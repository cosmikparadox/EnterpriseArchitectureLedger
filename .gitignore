.venv/
.venv312/
__pycache__/
*.pyc
data/
scratch/

# In-flight streaming output. Committed as a completed artefact once the
# run finishes; partial snapshots are noise.



# Canonical source documents fetched from the author's Drive for this session.
# NEVER COMMITTED: this repository is PUBLIC and these are unpublished
# framework documents. sim/README.md records the file ids and byte counts
# so the retrieval is reproducible without redistributing the sources.
sim/sources/
